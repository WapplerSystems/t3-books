plugin.tx_wsbooks {
    view {
        # cat=plugin.tx_wsbooks/file; type=string; label=Path to template root (FE)
        templateRootPath = EXT:ws_books/Resources/Private/Templates/
        # cat=plugin.tx_wsbooks/file; type=string; label=Path to template partials (FE)
        partialRootPath = EXT:ws_books/Resources/Private/Partials/
        # cat=plugin.tx_wsbooks/file; type=string; label=Path to template layouts (FE)
        layoutRootPath = EXT:ws_books/Resources/Private/Layouts/
    }
    persistence {
        # cat=plugin.tx_wsbooks//a; type=string; label=Default storage PID
        storagePid =
    }
}
